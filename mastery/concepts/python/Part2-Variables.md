# Variables & Data Types!
## Variables
What is a variable? Basically, in simple terms, it is simply a container to hold a value (Container, inside the container are apples).

To demonstate this:

```Python
variable_name = "Hello World!"
```

think of it this way, the container named `variable_name` holds a item named `"Hello World!"` and because of that, 